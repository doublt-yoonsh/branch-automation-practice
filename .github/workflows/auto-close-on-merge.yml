name: Auto Close Issue on Merge
on:
  pull_request:
    types: [closed]
    branches: [main, dev]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  auto-close:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Close linked issue and delete branch
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prBody = context.payload.pull_request.body || '';
            const prTitle = context.payload.pull_request.title || '';
            const targetBranch = context.payload.pull_request.base.ref;
            const sourceBranch = context.payload.pull_request.head.ref;
            
            const issueMatches = [
              ...prBody.matchAll(/(?:closes|fixes|resolves|close|fix|resolve)\s+#(\d+)/gi),
              ...prTitle.matchAll(/\[#(\d+)\]/gi),
              ...sourceBranch.matchAll(/(?:feat|hotfix|config)\/(\d+)/gi)
            ];
            
            const issueNumbers = [...new Set(issueMatches.map(match => parseInt(match[1])))];
            
            console.log(`Merged PR into ${targetBranch}, found issues: ${issueNumbers.join(', ')}`);
            
            for (const issueNumber of issueNumbers) {
              try {
                await github.rest.issues.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  state: 'closed'
                });
            
                console.log(`Closed issue #${issueNumber}`);
            
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  body: `🎉 이 이슈는 PR #${context.payload.pull_request.number}이 \`${targetBranch}\` 브랜치로 머지되면서 자동으로 닫혔습니다.`
                });
            
              } catch (error) {
                console.error(`Failed to close issue #${issueNumber}:`, error);
              }
            }
            
            if (sourceBranch.match(/^(feat|hotfix|config)\/\d+$/)) {
              try {
                await github.rest.git.deleteRef({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  ref: `heads/${sourceBranch}`
                });

                console.log(`Deleted branch: ${sourceBranch}`);
              } catch (error) {
                console.error(`Failed to delete branch ${sourceBranch}:`, error);
              }
            }

  backmerge:
    if: github.event.pull_request.merged == true && github.event.pull_request.base.ref == 'main'
    runs-on: ubuntu-latest
    needs: auto-close
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Backmerge main to dev
        id: backmerge
        run: |
          git checkout dev
          git pull origin dev

          if git merge origin/main --no-edit; then
            git push origin dev
            echo "status=success" >> $GITHUB_OUTPUT
          else
            git merge --abort
            echo "status=conflict" >> $GITHUB_OUTPUT
          fi

      - name: Create issue on conflict
        if: steps.backmerge.outputs.status == 'conflict'
        uses: actions/github-script@v6
        env:
          NOTIFY_USERS: ${{ vars.BACKMERGE_NOTIFY_USERS }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.payload.pull_request.number;
            const prTitle = context.payload.pull_request.title;
            const prAuthor = context.payload.pull_request.user.login;
            const prUrl = context.payload.pull_request.html_url;

            // 알림 대상 사용자 목록
            const notifyUsers = process.env.NOTIFY_USERS || prAuthor;
            const userList = notifyUsers.split(',').map(u => u.trim());
            const mentions = userList.map(u => `@${u}`).join(' ');

            const issueBody = `## 백머지 충돌 발생

| 항목 | 내용 |
|------|------|
| PR | #${prNumber} ${prTitle} |
| 작성자 | @${prAuthor} |
| 소스 | \`main\` |
| 타겟 | \`dev\` |

### 알림 대상
${mentions}

### 해결 방법
\`\`\`bash
git checkout dev
git pull origin dev
git merge origin/main
# 충돌 해결 후
git push origin dev
\`\`\`

### 관련 PR
${prUrl}`;

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `[Backmerge] main → dev 충돌 해결 필요 (PR #${prNumber})`,
              body: issueBody,
              assignees: userList,
              labels: ['backmerge']
            });

      - name: Comment on success
        if: steps.backmerge.outputs.status == 'success'
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.payload.pull_request.number;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              body: `✅ **백머지 완료**: \`main\` → \`dev\` 백머지가 자동으로 완료되었습니다.`
            });